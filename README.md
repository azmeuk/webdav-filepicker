# Run the project

You need [uv](https://docs.astral.sh/uv/getting-started/installation) on your system to run the project. Then:

```bash
uvx honcho start
```

Open the file provider at http://localhost:5000
Open the client at http://localhost:5001
